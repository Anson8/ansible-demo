---
- hosts: all
  gather_facts: no

  tasks:

  - name: install ssh key
    authorized_key: user=ops-admin
                    key="{{ lookup('file', '/home/icx-admin/.ssh/id_rsa.pub') }}"
                    state=present